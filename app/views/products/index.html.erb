<!-- Page Content -->
<div class="container">

  <div class="row">

    <%= render 'categories/cateogories_panel' %>

    <div class="col-md-9">
      <div class="row">
        <% if @products.blank? %>
            <div class="container">
              <br/><hr>
              <h3>Lo sentimos, no hay productos que contengan el término '<%= params[:search] %>'.</h3>
              <hr>
            </div>
        <% else %>
            <%= render 'products/products_toList' %>
        <% end  %>
      </div>
    </div>
  </div>
</div>